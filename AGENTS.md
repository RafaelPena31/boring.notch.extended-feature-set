# Local development

Use `./scripts/install-local.sh` for every local boringNotch build and installation. Do not manually re-sign or copy an app into `/Applications`; the script preserves and checks the different entitlements of the app and XPC helper.
